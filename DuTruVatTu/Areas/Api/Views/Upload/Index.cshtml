
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    @using (Html.BeginForm("Upload", "Upload", FormMethod.Post, new { enctype = "multipart/form-data" }))
            {
        <div class="col-md-3">
            <label>Nhập điểm bằng file Excel</label>
            <input name="file" type="file" required class="file" />
        </div>
                <div class="col-md-3">
                    <button type="submit" class="btn btn-primary" style="margin:5px">Nhập điểm</button>
                </div>
    }
</body>
</html>
